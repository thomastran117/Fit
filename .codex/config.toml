[mcp_servers.rentify]
command = "docker"
args = ["compose", "--profile", "mcp", "run", "--rm", "-T", "mcp"]
startup_timeout_sec = 30
tool_timeout_sec = 120

[mcp_servers.rentify.env]
RENTIFY_PAT = "${RENTIFY_PAT}"
